---
description: Show whether shadow-history capture is enabled for the current workspace, and where the shadow repo lives.
when_to_use: Use when the user asks "is history enabled", "is history running", or "history status".
allowed-tools: Bash
---

```!
bash "${CLAUDE_PLUGIN_ROOT}/bin/history" status
```
